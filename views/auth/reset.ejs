<%- include('../includes/head.ejs') %>
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <main>
        <div class="container my-4">
            <% if (errorMessage) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= errorMessage %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <% } %>
            <% if (validationErrors.length) { %>
            <% for( let index = 0; index < validationErrors.length; index++ ) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= validationErrors[index].msg %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <% } %>
            <% } %>
            <form action="/reset" method="POST">
                <div class="form-group">
                    <label for="email">Email address</label>
                    <input class="form-control
                        <% if (validationErrors.length) { %>
                        <%= validationErrors.find(e => e.param === 'email') ? 'is-invalid' : 'is-valid' %> <% } %>"
                        id="email" aria-describedby="emailHelp" placeholder="Enter email address." type="email"
                        name="email" id="email" value="<%= prev.email %>">
                </div>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button type="submit" class="btn btn-dark">Reset Password</button>
            </form>
            <div class="container">
                <div class="row  justify-content-center">
                    <a class="btn btn-secondary" href="/login">Login</a>
                    <div class="mx-2"></div>
                    <a class="btn btn-secondary" href="/signup">Signup</a>
                </div>
            </div>
        </div>
    </main>

    <%- include('../includes/end.ejs') %>